const costomer = ['m','f', 'm', 'm', 'f', 'm','f', 'm', 'm', 'f'];
const m = [];
const f= [];

for(let x of costomer){
    if(x == 'm')[
        m.push(x)
    ]
    else{
        f.push(x)
    }
}
console.log(m);
console.log('Number of Male',m.length);
console.log(f);